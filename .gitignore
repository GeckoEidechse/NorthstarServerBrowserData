# Any JSON file in the repo is likely from extracting an archive
# Given the large number of total JSON files, tracking them in git quickly becomes problematic
*.json

# Ignore images, as they were likely generated by some analysis script
*.png
*.jpg

# Python virtual environments
*env/
